\documentclass[tikz]{standalone}
\usepackage{tikz}
\usetikzlibrary{tensornetwork}

\begin{document}
\begin{tikzpicture}
	% Bottom layer
	\foreach \x in {1,...,5}
	{
		\foreach \y in {1,...,5}
		{
			\node[tensor=green!20!blue!50!white,minimum size=6mm] (A\x\y) at (\x,\y) {};
			\draw[tleg] ($ (A\x\y.225) + (45:0.06) $) -- +(225:0.35)  ;
			\draw[tleg] ($ (A\x\y.225) + (45:0.06) +(225:0.35) $) --  +(0.15,0.05)  ;
		}
	}
	\foreach \x in {1,...,5}
		\foreach \y [remember=\y as \lasty (initially 1)] in {2,...,5}
			\draw[tleg] (A\x\lasty) -- (A\x\y);
	\foreach \y in {1,...,5}
		\foreach \x [remember=\x as \lastx (initially 1)] in {2,...,5}
			\draw[tleg] (A\lastx\y) -- (A\x\y);
	% TOP layer
	\foreach \x in {1,...,5}
	{
		\foreach \y in {1,...,5}
		{
			\node[tensor=green!50!blue!50!white,minimum size=6mm] (B\x\y) at ($ (\x,\y) + (0.15,0.05) $) {};
			\draw[tleg] ($ (B\x\y.225) + (45:0.06) $) -- +(225:0.35) ;
		}
	}
	\foreach \x in {1,...,5}
		\foreach \y [remember=\y as \lasty (initially 1)] in {2,...,5}
			\draw[tleg] (B\x\lasty) -- (B\x\y);
	\foreach \y in {1,...,5}
		\foreach \x [remember=\x as \lastx (initially 1)] in {2,...,5}
			\draw[tleg] (B\lastx\y) -- (B\x\y);
			\node[tensor=red!50!white, minimum size=3mm] (X) at (3.6,2.5) {} ;
\end{tikzpicture}
\end{document}
